<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd">

	<!-- NOTE, use new Spring bean configuration file when creating new file, 
		to make the above information populated by default (have to select the bean 
		namespace you want. Also, add the context namespace using the defaults given 
		in the namespace at the bottom of this file -->

	<context:annotation-config/>

	<context:component-scan base-package="com.pluralsight"/>
	
	<!--  3 types of annotations @Component (regular beans and POJO, @Service (business logic), @Repository (data access tier) -->
	
	

</beans>
